metadata:
  name: test-template
spec:
  build:
    functionSourceCode: {{ .SourceCode }}
  description: "Returns the returned number"
  env:
    - name: RETURNED_TEXT
      value: {{ .ReturnedText }}
  handler: "main:handler"
  runtime: "python:3.6"
  minReplicas: 1
  maxReplicas: 1
